apiVersion: v1
kind: Secret
metadata:
  name: codex-proxy
  namespace: codex-proxy
type: Opaque
stringData:
  # Replace before applying. This key protects API and admin endpoints.
  proxy-api-key: replace-with-a-long-random-value
